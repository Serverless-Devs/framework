const { oss } = require('../src/index');

describe('事件函数', () => {
  test('测试OSS 事件', async () => {
    // const handler = oss.onObjectCreated({
    //   bucketName: 'my-bukect',
    //   handler: (request) => {
    //     return {
    //       success: 'true',
    //     };
    //   },
    // });
    // TODO: test
  });
});
